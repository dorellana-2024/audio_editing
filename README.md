# Automatize audio editing

## Requirements
- Docker
- Docker Compose
(*) Install Docker: https://www.docker.com/products/docker-desktop/

## Run the project
```bash
git clone https://github.com/YOUR_USERNAME/my-python-project.git
cd my-python-project
docker compose up --build
```
